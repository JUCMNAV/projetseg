package intermediateWorkflow;


require "platform:/resource/aoUrnToRam/src/iwToStepView/_Ref.kmt"

using kermeta::standard

aspect class IwCustomizableNode
{
	method getIsCustomizable():Boolean is do
		result:=true
	end
	
	method getImageName():String is do
		result:=if(isShared) then "SharedResp24.gif" else "Resp16.gif" end
	end
}